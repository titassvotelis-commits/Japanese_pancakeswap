/**
 * Path discovery re-exports — pair combinations are built in hooks/Trades.ts (useAllCommonPairs).
 * Reserves are merged from local + Pancake factories via hooks/usePairsWithSource.ts.
 */
export { MAX_ROUTE_HOPS } from './constants'
