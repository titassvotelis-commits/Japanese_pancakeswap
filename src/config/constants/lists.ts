/** Optional remote lists (Manage lists). Core tokens ship in metakey-default.tokenlist.json. */
export const UNSUPPORTED_LIST_URLS: string[] = []

export const DEFAULT_LIST_OF_LISTS: string[] = [...UNSUPPORTED_LIST_URLS]

export const DEFAULT_ACTIVE_LIST_URLS: string[] = []
